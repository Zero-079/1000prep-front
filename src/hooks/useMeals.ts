// NUEVO: Hook que maneja la lógica de fetch
// Responsabilidades:
// - Estado de carga (loading, error, data)
// - Llamada a API cuando monta el componente
// - Manejo de errores
// - Refetch manual si es necesario

export const useMeals = (date?: string, mealType?: string) => {
  // Retorna: { meals, loading, error, refetch }
}
