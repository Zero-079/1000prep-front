// NUEVO: Servicio API para comidas
// Responsabilidades:
// - Llamadas GET /meals/daily
// - Llamadas GET /meals/:id
// - Manejo de errores
// - Tipos TypeScript sincronizados con backend

export const getMealsDaily = async (date?: string, mealType?: string) => {
  // Lógica aquí
}

export const getMealById = async (id: string) => {
  // Lógica aquí
}
